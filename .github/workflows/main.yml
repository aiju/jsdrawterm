name: Build

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Setup Emscripten
      uses: lovasoa/setup-emscripten@v1
      with:
        emscripten-version:
    - name: build
      run: |
        make -j
        
